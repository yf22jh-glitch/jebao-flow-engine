# run: 2026-09-01-current-baseline-78058401

- 목적(한 문장): 어떤 다음 live-write보다 먼저 두 Pro의 현재 independent controls와 exact
  schedule image를 device-state write 없이 다시 보존하고 역할 B의 restore admission을
  재판정한다.
- 이 실행이 답하려던 질문: "2026-08-30 baseline에서 복원을 막았던 역할 B의 latent manual
  `Flow=89`가 현재도 남아 있는가?"
- 실행한 commit SHA: `89119bc3aab1147ac97a0092b2af38a84d7effb3`
- 사용한 명령·주요 파라미터:
  - exact commit의 fresh detached clean source에서 project를 설치하지 않고
    `python3.12 -B -P -m jebao_flow.read_only_collector_cli`를 직접 실행
  - `--samples 2 --cadence-seconds 30 --timeout 5`
  - source-attested acquisition-only scope,
    `q2_boundary_classification=not_authorized`
  - 실행 전 focused suite 85개 PASS:
    `test_read_only_collector.py`, `test_read_only_collector_cli.py`,
    `test_source_attestation.py`, `test_protocol_session.py`
  - private config는 `dry_run=true`, 두 대상 모두
    `allow_hardware_writes=false`인 owner-only 복사본만 사용
  - recovery container는 실행 전부터 정지 상태였으며, collector가 시작·정지하지 않고 그
    상태를 그대로 보존
  - collector는 private plan·raw·series 산출물을 fsync했지만 장비 control frame은 보내지
    않는 source-attested read-only 경로
- 종료 지점(stage / failure code):
  `pilot_completed_all_acquisitions_accepted`, exit 0, 별도 offline verify PASS
- 장비가 실제로 돌려준 값:
  - 2/2 pair accepted, predicate rejected 0, read failure 0
  - 두 ordinal에서 역할별 값과 schedule image digest가 각각 동일
  - 역할 A: `ON / TimerON / independent / constant / Flow=30 / Frequency=32`;
    sample 시점 active slot은 `sine / AutoFlow=45 / AutoFreq=40`
  - 역할 B: `ON / TimerON / independent / random / Flow=89 / Frequency=34`;
    sample 시점 active slot은 `sine / AutoFlow=65 / AutoFreq=80`
  - 역할 B의 같은 raw에서 재도출한 48-slot non-feed schedule Flow 범위는 `50..80`
  - schedule image SHA-256:
    - 역할 A: `60edc697ebe4492259c6df01b9f92726af5648343f9b8e24950d721fa8f56b99`
    - 역할 B: `c50331c20d220b2c51b2022863d29e5fc760d7ede71c299ec4f606e597d29426`
  - 증거 등급:
    - exact explicit-reply wire frame과 그 digest, 그리고 offline verifier가 그 raw를 다시
      디코딩해 `Flow=89`와 schedule `Flow=50..80`을 재도출할 수 있는 입력:
      **(a) preserved raw artifact**
    - `89 > 80`은 위 raw에서 재도출한 두 값을 비교한 admission 판정이며 wire에 직접
      들어 있는 값은 아님
    - source attestation, host UTC·monotonic 시각, identity binding, plan·series claim과
      raw 재계산에 일치한 persisted decoded state summary:
      **(b) preserved structured/durable daemon artifact**
    - recovery가 이미 정지 상태였고 pre/post 상태가 같았다는 운영 확인:
      **(c) reconstructed operator observation**
  - opaque artifact id:
    - plan: `JFP-60548177648bbb3cbed731b2ebff841f`
    - series: `JFS-780584017739a4b193c5bb157d557802`
  - UTC span (시작 / 종료): `2026-09-01T14:19:19.581272Z` /
    `2026-09-01T14:20:11.661787Z`
  - SHA-256 identity binding:
    - `4b60797d5fa86d879410c03a4c508665ee0f7a693fc54bd67c462c9a664bafea`
    - `97040a73bd12c3295dfb1cef77f6435949e78cc9e65bd2b28d93742e1e88cb1c`
  - artifact digest (SHA-256):
    - plan: `04c3dff7fcf74ba6bf92347fa8b508bcbc76432c4292ee92deb42c97fe217c0d`
    - series: `d4d52f82791e4ccdde6fcf79bea4f34e64a1e9b198394ee8604fac96873cd37c`
- 원복 검증 결과(어떤 수준의 확인이었는지 명시):
  - 이 실행은 source-attested write-free collector라 장비 원복 대상이 없다.
  - 실행 전후 private config digest와 hardware-safety content-set digest가 각각 동일했고,
    TCP 12416 established connection은 실행 전후 0이었다.
  - recovery container는 실행 전부터 정지 상태였고 실행 후에도 정지 상태로 보존했다.
- **이 실행으로 새로 확정된 사실:**
  - 현재 두 Pro에서 exact `89119bc` collector가 2 pair / 4 explicit-reply raw frame을 모두
    보존하고 offline verify를 통과한다.
  - 역할 B의 latent manual `Flow=89`와 non-feed schedule `Flow=50..80`이
    2026-09-01 현재 두 fresh sample의 raw에서 다시 재도출됐다. 두 값을 비교한
    `89 > 80` restore admission FAIL은 과거 baseline만의 추정이 아니라 현재 live state에도
    그대로 적용된다.
  - 두 schedule image digest는 값이 커밋된 2026-09-01 corrected-run restore 기록과
    byte-exact하게 같고, manual controls와 `TimerON / independent`도 일치한다. active slot
    값의 차이는 같은 TimerON schedule의 시간 진행과 정합하지만 물리 파형을 증명하지는 않는다.
- 이 실행으로 확정하지 못한 것:
  - acquisition-only scope라 Q2-target은 계속 `UNKNOWN`이다. ASYNC role write나 master/slave
    동작 관측은 수행하지 않았다.
  - sample 시점 protocol state 외의 물리 유량·파형·위상은 측정하지 않았다.
  - 휴대폰 앱이 완전히 종료돼 있었는지는 서버에서 증명하지 않았다.
  - source attestation과 import·transport 테스트는 device-state write 0을 뒷받침하지만,
    수신 raw 자체가 송신 프레임 pcap인 것은 아니다.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것:
  - 이 artifact는 **pre-normalization baseline**이며 선행조건 2를 해소하지 않는다. 앱
    정규화가 역할 B의 manual controls를 바꾸므로 정규화 직후부터 이 artifact를 복원 기준으로
    사용하지 않는다.
  - 어떤 ASYNC role write보다 먼저 on-site hardware approver가 별도로 승인한 앱 operation으로
    역할 B의 latent manual Flow를 안전 범위로 정규화한다.
  - 앱을 완전히 종료한 뒤 같은 write-free collector로 fresh explicit baseline을 다시 보존하고,
    그 baseline으로 standalone restore admission을 재판정한다.
  - admission이 PASS한 뒤에만 별도 승인 범위에 따라 ASYNC transport probe 1회를 검토한다.

## 실행 전 준비 중단 기록

이 성공 실행 전에 준비 두 번이 모두 장비 discovery·TCP 연결 전에 fail-closed 됐다. 첫 시도는
recovery가 실행 중이어야 한다는 과거 preflight 가정을 사용했고, 두 번째는 정지 상태를
보존하도록 바꾼 뒤 host-mounted private config의 존재를 비특권 `test -f`로 확인해 권한
경계에서 종료됐다. 세 번째 준비에서 그 확인만 `sudo test -f`로 바로잡았고 config 내용이나
장비 상태는 바꾸지 않았다. 앞선 두 시도 모두 exact source focused suite 85개를 통과했지만
collector network call에 진입하지 않았고 장비 write는 0회였다. 따라서 별도 device-contact
operation으로 세지 않는다. **이번 기록의 device-contact operation은 성공한 collector 1회뿐이다.**
