# run: 2026-08-28-pilot-2bd1bf97

- 목적(한 문장): 두 Local Wavemaker Pro에서 write-free collector가 explicit reply 원본을
  빠짐없이 보존하고 오프라인에서 재검증할 수 있는지 확인한다.
- 이 실행이 답하려던 질문: "Q2를 판정하기 전에 필요한 transport·timing collector가 실제
  장비에서 유효한 2-device 순서 시리즈를 만들 수 있는가?"
- 실행한 commit SHA: `f699cfbec9c3beb87c92ef9ff17803d562a3bd36`
- 사용한 명령·주요 파라미터:
  - fresh detached clone, external venv, project 미설치
  - systemd transient service의 direct `ExecStart` argv(private 값 치환): `python3.12 -B -P -m
    jebao_flow.read_only_collector_cli --config <private-config> --first <logical-role-a>
    --second <logical-role-b> --output-root <private-artifact-root> --samples 18
    --cadence-seconds 20 --collector-commit
    f699cfbec9c3beb87c92ef9ff17803d562a3bd36 --timeout 5`
  - 명령에 생략된 기본값: discovery port 12414, control port 12416
  - `PYTHONNOUSERSITE=1`, `PYTHONDONTWRITEBYTECODE=1`, 새 external pycache
  - source-attested clean commit과 symlink-free real path
  - runtime source digest: `3421fa9194ca20d65c42a4634819d833ad1e77cf7fd0bab9dfbd0b39254a1aef`
  - write-free 경로 근거:
    - plan에 보존된 commit·runtime source digest와 실행 전·실행 시 source 재검증:
      **(b) preserved structured/durable daemon artifact**
    - 고정 argv·systemd 실행 결과와 같은 commit에서 PASS한
      `test_collector_import_graph_does_not_load_device_or_frozen_modules`,
      `test_real_transport_pilot_series_sends_no_control_frame`(focused 91 / full 1242 전체 PASS):
      **(c) reconstructed operator observation**
- 종료 지점(stage / failure code): `pilot_completed_all_acquisitions_accepted`, exit 0
- 장비가 실제로 돌려준 값:
  - 18/18 pair, 36/36 sample이 `accepted`
  - 36/36 원본 프레임이 explicit state reply이며 각 프레임의 exact wire bytes를 보존
  - 두 역할 모두 18/18 sample에서 `AutoMode`, `AutoFlow`, `AutoFreq`, `Linkage`,
    `TimerON`, `Mode`, `Flow`, `Frequency`를 디코딩
  - 역할별 schedule image digest는 18 sample 동안 각각 한 값으로 유지
  - pair completion gap: 최소 10,630.131 ms / 중앙값 10,852.383 ms / 최대 11,177.099 ms
  - 실제 pair 시작 간격: 최소 21,210.971 ms / 중앙값 21,912.105 ms /
    최대 22,528.489 ms
  - 장비 시계 값이 바뀐 sample 사이의 host 간격:
    - 역할 A: 최소 21,492.094 ms / 중앙값 22,483.128 ms / 최대 44,971.686 ms
    - 역할 B: 최소 21,208.158 ms / 중앙값 22,486.487 ms / 최대 44,960.209 ms
  - 증거 등급:
    - reply action과 status body exact bytes: **(a) preserved raw artifact**
    - host UTC·monotonic 시각, identity binding, plan·series claim:
      **(b) preserved structured/durable daemon artifact**
    - decode된 state summary(위 8개 필드와 schedule image digest)와 state observation:
      **(b) preserved structured/durable daemon artifact**
  - opaque artifact id:
    - plan: `JFP-aecea1ef99f878f4c4d984815a66c352`
    - series: `JFS-6b376f051804caf542ed8469c49ff868`
  - UTC span (시작 / 종료): `2026-08-28T13:32:06.385678Z` /
    `2026-08-28T13:38:41.576742Z`
  - SHA-256 identity binding:
    - `4b60797d5fa86d879410c03a4c508665ee0f7a693fc54bd67c462c9a664bafea`
    - `97040a73bd12c3295dfb1cef77f6435949e78cc9e65bd2b28d93742e1e88cb1c`
  - artifact digest (SHA-256):
    - plan: `e52036ed31ff301e969231d5c295c675098ae1d95071c03e3e85c58341b347a9`
    - series: `e72462acb7edeb82594620d9a08fe6886089292f465301ae67a5f0499d72d533`
- 오프라인 검증:
  - 같은 commit의 `PilotSeriesStore.load()`와 `verify_completed_series()`로 별도 재검증 PASS
  - ordinal 0~17, attempt 집합, pair/sample digest, raw digest, exact reply action,
    physical binding, count와 terminal marker를 모두 다시 계산해 일치 확인
  - verifier가 preserved raw를 다시 디코딩해 재계산한 `state_summary`와
    `state_observation`을 보존된 daemon claim과 동일한지 비교했다. 따라서 등급 (b) summary는
    등급 (a) raw에서 오프라인으로 재현 가능하다.
- 원복 검증 결과(어떤 수준의 확인이었는지 명시): 이 pilot은 위에 기록한 source-attested
  write-free 경로로 실행돼 schedule·role 원복 대상이 없다(이 성질의 증거 범위와 한계는 아래
  "확정하지 못한 것" 참조). 실행 전 중지한 recovery container는 systemd `ExecStopPost`로
  재기동됐고, 실행 후 running, pending recovery false, nonterminal intent·journal·temporary
  schedule·emergency-stop latch 없음, TCP 12416 연결 0을 재확인했다. 이 운영 검증은 당시
  명령 결과에 근거한 **(c) reconstructed operator observation**이다.
- **이 실행으로 새로 확정된 사실:**
  - source-attested write-free collector가 실제 두 Pro에서 18 pair의 prefix-complete durable
    series와 36개 exact explicit-reply raw frame을 만들고 오프라인 검증을 통과한다.
  - 요청 cadence 20초에서 현재 fresh-session/identity-read 절차의 실제 pair 시작 간격은
    21.2~22.5초이고, 순차 두 장비의 completion gap은 10.6~11.2초다. 이후 manifest의
    cadence·host-side pair-gap 후보는 이 preserved artifact를 근거로 headroom을 두고 정해야
    한다. 이 completion gap은 retry5의 **device `NowTime` skew 2초 gate와 다른 측정량**이므로
    둘을 같은 임계값으로 취급하지 않는다. 다만 현재 수집 경로에서 host-side 최대 pair gap을
    관측 최대 11.18초보다 작게 정하면 이번 정상 sample 중 일부가 무효가 되고, 관측 최소
    10.63초보다 작게 정하면 전부 무효가 된다.
  - 이전에 등급 (c)이던 약 22~25초 장비 시계 갱신 현상은 이번 6분 35초 구간에서 host 간격
    중앙값 약 22.48초로 다시 관측됐다. 이 파생 관측은 **(b)**이며, 장비 시계 값은 등급 (a)
    raw에서, host 간격은 등급 (b) monotonic timing에서 나온다. 다만 sample 자체가 약 21.9초
    간격이라 22.48초는 한 sample step, 44.97초는 두 step으로 alias된다. 따라서 이 결과는
    cadence·freshness 설계 입력으로 사용할 수 있지만 장비 고정 갱신 주기나 강제 manifest
    값으로 쓰지 않는다.
  - hardware-readiness가 Q2 판정에 쓰기로 정한 상태 필드가 두 역할의 18/18 sample에서 모두
    디코딩됐다. 그 필드 집합의 충분성은 이 실행이 판정하지 않는다.
  - 역할별 schedule image digest가 18 sample 동안 각각 한 값이었던 것은 관측 구간에 schedule
    write가 없었다는 제한적 방증이다. 약 22초 sample 사이의 write-restore와 schedule 외
    control 변경은 검출하지 못하므로 write-zero의 독립 증명으로 사용하지 않는다.
- 이 실행으로 확정하지 못한 것:
  - 이 실행의 validity scope는 `acquisition_only_not_q2_boundary`이고,
    `q2_boundary_classification=not_authorized`다. 따라서 Q2는 여전히 `UNKNOWN (PARKED)`이다.
  - 장비의 물리 유량·파형·위상은 측정하지 않았다.
  - 수신 raw는 송신 프레임의 pcap이 아니다. device-state write 0은 source-attested read-only
    session의 import graph와 transport test, 고정 실행 명령으로 뒷받침되지만 수신 raw만으로
    독립 증명하지는 않는다.
  - 서버 측 다른 controller는 0개였지만 휴대폰 앱 완전 종료 여부는 서버에서 증명할 수 없다.
  - 한 host·한 network path·한 시간대의 395.191초짜리 단일 실행(`n=1`)이다. 위 최소·최대에는
    tail 분석이나 운영 margin이 없으므로 그대로 manifest 상한으로 복사할 수 없다.
  - 전체 span이 300초 안정 구간 하한을 약 95초 넘겼을 뿐이라, 300초보다 큰 안정 구간 하한을
    이 artifact에서 도출할 수 없다. 그러려면 더 긴 read-only pilot이 필요하다.
  - 36/36 sample이 모두 accepted이고 retry policy가 `none`이어서 실제 장비에서
    `predicate_rejected`, `read_failure`, 5초 timeout 경로는 발생하지 않았다. failure artifact와
    boundary-exclusion 경로는 unit test 근거만 남아 있다.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것:
  - 이 artifact에서 cadence·최대 pair gap·freshness 후보와 필요한 headroom을 도출한다.
    boundary exclusion은 실제 경계를 포함한 별도 read-only 자료 또는 보수적 사전 규칙이,
    300초보다 큰 안정 구간 하한은 더 긴 pilot이 필요하다.
  - Q2 관측을 재개하려면 별도로 검증된 exact restore 수단과 앱 live-write 절차를 먼저
    충족해야 한다. 이 pilot을 근거로 동결된 native ASYNC write 하네스를 재개하지 않는다.

## 실행 전 준비 중단 기록

이 실기 전에 두 준비 시도가 장비 discovery·TCP 연결 전에 fail-closed 됐다. 첫 시도는 호스트의
`python3.12-venv` 부재, 두 번째는 비네트워크 provenance preflight의 잘못된 import symbol이
원인이었다. 두 디렉터리·clone·venv·artifact를 이 실행에 재사용하지 않았고, 위 결과는 세 번째
fresh runtime에서 얻었다. 두 준비 중단은 장비에 접촉하지 않아 별도 실기 횟수로 세지 않는다.
