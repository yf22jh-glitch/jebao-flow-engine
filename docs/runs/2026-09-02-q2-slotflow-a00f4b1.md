# run: 2026-09-02-q2-slotflow-a00f4b1

- 목적(한 문장): master와 slave에 같은 `Sine -> Constant` 경계와 서로 다른 슬롯별 Flow를
  백엔드 하네스로 한 번 적용해, `async_slave`가 master의 Mode·경계를 따르면서 자기
  `Flow 35 -> 47`을 적용하는지 관측하고 원 상태를 exact restore한다.
- 이 실행이 답하려던 질문: "master가 `Sine/F50/Flow40 -> Constant/Flow35`로 바뀔 때 slave가
  같은 Mode·경계를 따르면서 자기 `Sine/F50/Flow35 -> Constant/Flow47`을 적용하는가?"
- 실행한 commit SHA: `a00f4b100a1873977b6d45df8344ae32c8e457f8`
- 사용한 명령·주요 파라미터:
  - exact commit의 clean Linux/amd64 image
    `sha256:9ba483aa18f24d2ec918ce46b753d3006323143b3832b5fda5acd9fd20b6bfee`
  - master safe manual `Constant/30/F20`, slave safe manual `Constant/35/F20`
  - master A=`Sine/40/F50`, B=`Constant/35/wire F0`
  - slave A=`Sine/35/F50`, B=`Constant/47/wire F0`
  - 동일 device-local 경계 `2026-09-02T12:20:00`, master=`master`,
    slave=`async_slave`, complete observation epoch `900초`
  - 실행 전 macOS 전체 suite `1759 PASS / 3 FAIL`; 같은 image의 Linux suite `1762 PASS`.
    세 차이는 Linux에서 모두 통과해 host platform에 한정됨을 확인
- 종료 지점(stage / failure code):
  - monitor는 `2026-09-02T03:12:12.795125Z`부터
    `2026-09-02T03:27:13.055806Z`까지 전체 epoch를 수행했지만 valid post-boundary slave
    sample을 만들지 못했다.
  - durable verdict는 `verified_sample_count=0`; terminal outcome은
    `experiment_failed_restored`다. 이 실행의 판정은 **`UNKNOWN`**이며 native capability
    `FAIL`이 아니다.
  - ordered rollback 중 `control_disarm_unverified` 진단이 한 번 남았지만, 이어서
    `role_disarmed -> field_restored -> outer_restored`까지 terminal로 완료됐다. structured
    stage event는 어느 participant의 확인이 불명확했는지 보존하지 않았고, 아래 두 fresh
    collector의 두 장비 byte-exact 검증이 그 미확인 상태를 해소했다.
- 장비가 실제로 돌려준 값:
  - live private raw sink에는 header 1개와 exact state frame `822`개가 보존됐다.
    - master: action `0x03` 410개 + action `0x04` 1개
    - slave: action `0x04` 411개 + action `0x03` 0개
  - master raw는 계획한 경계를 보였다.
    - 경계 전 199개: `Linkage=master`, `Sine/AutoFlow40/AutoFreq50`
    - 경계 후 212개: `Linkage=master`, `Constant/AutoFlow35`; 첫 frame은 action `0x04`,
      device-local `NowTime=12:20:00`, host 수신 완료 `03:20:07.246461Z`였다. 바로 다음
      action `0x03` frame도 같은 `NowTime`과 tuple을 `03:20:09.255213Z`에 반환했고, 마지막
      `12:27:00` frame까지 action `0x03`으로 같은 Constant/35를 보존했다.
  - slave의 411개 frame은 모두 `SwitchON=true`, `TimerON=true`, `Linkage=async_slave`,
    manual·Auto `Sine/35/F50`을 담았다. 같은 raw의 temporary schedule image digest는
    `8672d1f98d2da523a0b2d424f8721409eeceeabcec1fa79fd5a317c1e08b7c5c`로 411개 모두 같았고,
    slot A=`00:00–12:20 Sine/35/F50`, slot B=`12:20–23:59 Constant/47/F0`가 실제 저장돼
    있었다. 다만 **frame 내부 `NowTime`이 모두 경계 전**이었다.
    - 첫 slave frame: host `03:12:16.744670Z`, device-local `12:12:00`
      (UTC 환산 뒤 약 16.7초 지연). 이 frame-local 시각은
      `field_verified=03:11:48.209762Z`와 `timer_on_armed=03:11:52.502329Z` 뒤이고, frame
      자체도 `TimerON=true`, temporary schedule과 `Linkage=async_slave`를 함께 담았다.
      따라서 경계 전 staged A 상태 적용은 (a) raw로 확인된다.
    - 마지막 slave frame: host `03:27:11.052837Z`, device-local `12:19:00`
      (UTC 환산 뒤 약 491.1초 지연)
    - host 수신은 15분 동안 진행됐지만 device-local 시각은 7분만 전진했다. 따라서 이
      frame들은 post-boundary 상태가 아니라 session에 쌓여 있던 report backlog와 정합한다.
      경계 후 실제 slave 상태가 `Sine/35`였다고 해석하지 않는다.
  - durable sample은 경계 전 pair 한 개만 보존했다: master `Sine/40/F50` action `0x03`,
    slave `Sine/35/F50` action `0x04`. post-boundary pair는 `0`개다.
  - 증거 등급:
    - live raw 822개와 post-restore collector의 exact reply frame, 그리고 그 frame에서
      재디코딩한 state·control·schedule: **(a) preserved raw artifact**
    - terminal intent, stage event, rollback evidence, collector plan·series·source attestation:
      **(b) preserved structured/durable daemon artifact**
    - 현장 물리 차단 수단 확인, old terminal intent archive 이동, recovery runtime 재기동과
      TCP·lease 확인: **(c) reconstructed operator observation**
    - 아래 앱 명령 추적은 장비가 만든 증거가 아니라 cached Pro template의 정적 `U` 분석이다.
      (a)/(b)/(c)로 승격하지 않는다.
  - opaque artifact id와 digest:
    - live raw `JFR-60203de0b0da`:
      `421b32ee0649aea4eac4c973b16496f9391f9b86f4dc4a8d5903ec1a78a969ee`
    - terminal intent logical id `native-linkage-intent`:
      `a72e403bab9321fcedc1a23b24df9705eb0657b12f12a427b840d031e01819c3`
    - pre-write baseline series `JFS-c0f41e65c0627cbc3926ed068c049afd`:
      `9edf37d58da426fa54e55e5fdc712322660336a8acb99102f9e4d867368b4392`
    - post-restore series 1 `JFS-cb80a98978a11c9855ebb9e4c2529ca5`:
      `11cace699087a80fcd404fe4f8e20c88559352681a21e0106988c267b3a80c3a`
    - post-restore series 2 `JFS-973f025caee2f3301fb7cbea340bec90`:
      `ea1c57d8a16ede6964d6674281c87dc95bc8593032131daf77a1f647818f3382`
  - UTC span (시작 / 종료):
    - terminal intent: `2026-09-02T03:10:52.777926Z` /
      `2026-09-02T03:27:59.902492Z`
    - live raw: `2026-09-02T03:12:15.954870Z` /
      `2026-09-02T03:27:11.052837Z`
    - restore verify 1: `2026-09-02T03:28:53Z` / `2026-09-02T03:29:45Z`
    - restore verify 2: `2026-09-02T03:29:58Z` / `2026-09-02T03:30:50Z`
  - SHA-256 identity binding(세 collector 공통):
    - `4b60797d5fa86d879410c03a4c508665ee0f7a693fc54bd67c462c9a664bafea`
    - `97040a73bd12c3295dfb1cef77f6435949e78cc9e65bd2b28d93742e1e88cb1c`
- 앱 명령과 하네스 명령의 사후 교차검증:
  - 검사한 세 cached Pro template의 관련 UI dataflow는 동일했다.
  - 앱의 Linkage 화면은 각 선택마다 partial object 하나를 보낸다. role 선택은
    `{Linkage: ...}`만 보내고 `TimerON`, `Flow`, `Mode`, `Frequency`, schedule을 합성하지
    않는다. slave Flow slider도 별도 `{Flow: ...}` write다.
  - 앱 UI에서 `independent` 상태의 Slave 버튼은 먼저 `Linkage=2`(`sync_slave`)를 보낸다.
    Sync/Async 버튼은 현재 role이 2 또는 3일 때만 활성화되므로 Async를 선택하면 이어서
    `Linkage=3`을 별도 전송한다. 즉 앱의 UI 경로는 `0 -> 2 -> 3`이다.
  - 이번 하네스의 `write_linkage(async_slave)`는 partial `Linkage=3`을 한 번 보내
    `0 -> 3`으로 직접 전환했다. final datapoint는 같지만 앱 UI의 전이 sequence와 동등하지 않다.
  - intermediate `Linkage=2`가 펌웨어에서 ASYNC schedule 동작에 필수인지는 아직 증명하지
    않았다. 2026-08-30 attempt 05도 같은 direct `0 -> 3` 하네스 경로에서 slave `32 -> 40`을
    관측했으므로, 이번 미관측의 원인이 intermediate role 생략이라고 단정할 수도 없다. 이 차이는
    현재 결과를 `FAIL`로 승격하지 못하게 하는 condition-construction gap이다.
- 원복 검증 결과(어떤 수준의 확인이었는지 명시):
  - terminal intent는 rollback failure·recovery reason 없이 `outer_restored`를 기록했다: **(b)**.
  - writer 종료 뒤 서로 다른 두 fresh source-attested collector를 실행했다. 두 회 모두
    `2/2 pair`, `4/4 action 0x03`, offline verify PASS였고 pre-write baseline과 아래 값이
    byte-exact하게 일치했다: **(a)**.
    - 역할 A: `ON / TimerON / independent / constant / Flow30 / Frequency32`, schedule digest
      `60edc697ebe4492259c6df01b9f92726af5648343f9b8e24950d721fa8f56b99`
    - 역할 B: `ON / TimerON / independent / random / Flow89 / Frequency34`, schedule digest
      `c50331c20d220b2c51b2022863d29e5fc760d7ede71c299ec4f606e597d29426`
  - locked recovery runtime은 검증 뒤 `dry_run=true`, observer disabled, write-enabled device
    `0` 상태로 다시 시작했다: **(c)**.
- **이 실행으로 새로 확정된 사실:**
  - temporary schedule write·read-back과 `TimerON` arm 뒤 master는 device-local 경계에서
    `Sine/40/F50 -> Constant/35`로 실제 전환했다.
  - slave의 모든 보존 frame에서도 `TimerON=true`, `Linkage=async_slave`와 exact temporary
    schedule의 B slot `Constant/47/F0`가 확인됐다. role 진입이 Timer를 끄거나 B slot 저장을
    지웠다는 가설은 이 epoch의 pre-boundary raw와 맞지 않는다.
  - 이 run의 monitor read는 slave `0x03`을 한 번도 반환받지 못했고 action `0x04` report
    411개를 얻었다. 다만 `accept_reports=True` 경로는 세션에 쌓인 `0x04`를 요청 응답보다 먼저
    선택할 수 있으므로, 이 사실로 slave가 strict read에 응답하지 않았다고 추론하지 않는다.
    `async_slave` 상태의 explicit reply 거동은 계속 `UNKNOWN`이다. device-local 시각 지연은
    약 16.7초에서 491.1초로 발산했고, 경계 후 자격을 가진 slave frame은 0개였다. 같은 세션에서
    epoch만 더 늘려 backlog를 따라잡을 수 있다는 근거도 없다.
  - frame-local `NowTime` boundary gate가 stale `0x04`를 post-boundary sample로 승격하지 않아
    실제 `FAIL`을 거짓으로 만들지 않았다.
  - 앱 UI의 ASYNC 전환은 `independent -> sync_slave -> async_slave`인데 하네스는
    `independent -> async_slave`였다. 현재 실기는 앱과 명령 sequence가 동등하지 않다.
  - ordered rollback과 두 fresh collector에서 원 controls와 두 432-byte schedule image의 exact
    restore가 확인됐다.
- 이 실행으로 확정하지 못한 것:
  - 경계 후 slave의 실제 Mode·Flow, slave가 자기 B slot `Constant/47`을 적용했는지,
    intermediate `sync_slave` 전환이 결과를 바꾸는지는 모두 `UNKNOWN`이다.
  - `Sine/35`를 담은 stale report는 경계 후 고정 상태의 증거가 아니므로 `FAIL` 근거로 쓰지 않는다.
  - 이 계획에서는 slave pre-slot과 safe manual이 모두 `35`라 classifier의
    `COMMON_MANUAL_FLOW`와 `A_SLOT_HOLD` 원인 가설이 값만으로 겹친다. 그러나 이번에는 valid
    post-boundary pair 자체가 없으므로 어느 label도 실제 원인이나 비전이 증거로 읽지 않는다.
  - protocol-reported 값 밖의 물리 유량·파형·위상과 장기간 반복 신뢰도는 측정하지 않았다.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것:
  - `13c5068`의 단회 해제는 이 terminal restore와 두 fresh 검증으로 소진됐다. 자동 재시도하지
    않고 §1 동결을 다시 적용한다.
  - 추가 실기가 필요하면 별도 승인·별도 해제 커밋으로 slave role을 앱과 같은
    `independent -> sync_slave -> async_slave` exactly-once sequence로 고정한다.
  - 같은 실행에서 slave report backlog를 fresh session 또는 명시적 drain으로 제거하고,
    device-local `NowTime`이 staging 완료 뒤이면서 경계 후인 frame을 보존해야 한다. stale
    `0x04`를 판정 sample로 승격하거나 같은 role write를 재전송하지 않는다.

## 실행 전 old terminal intent archive

이전 2026-09-01 실행의 terminal intent가 fixed signature 변경 뒤 현행 loader와 맞지 않아,
이번 preflight 전에 loader가 읽지 않는 archive 이름으로 같은 safety volume 안에서 이동했다.
대상은 terminal이고 journal 3종·emergency latch가 모두 없으며, 커밋된 이전 run 기록의 digest
`afe9859bc3c668b8d1d64170118198436d5fc2324483afc0d548ff3402643ef9`와 일치했다. 이동 전후
digest, mode `0600`, uid `100`, gid `101`, link count `1`, byte length `27545`가 같았다. volume을
잡은 process가 없음을 먼저 확인하고 파일과 양쪽 directory를 fsync했다. 이 조작은 하네스가
만든 raw가 아니라 **(c) reconstructed operator observation**이다.

이번 건은 persisted intent의 유효성이 fixed signature 상수에 결합돼 있다는 운영 위험도
드러냈다. **비-terminal intent가 하나라도 있으면 fixed signature를 바꾸지 않는다.** 이번 run이
비-terminal로 끝났다면 `a00f4b1` image로만 attended recovery를 수행해야 했지만, 실제 run은
terminal restore와 별도 raw 검증까지 완료했다.
