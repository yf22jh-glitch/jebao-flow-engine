# run: 2026-08-30-q2-attempt-05

- 목적(한 문장): 서로 다른 slave A/B `AutoFlow`를 실제 ASYNC 경계에서 900초 관측하고
  byte-exact 원복을 확인한다.
- 이 실행이 답하려던 질문: ASYNC slave가 master를 따라가지 않고 자기 B 슬롯 출력을 독립
  적용하는가?
- 실행한 commit SHA: `b72cb33635d7b41ea6ead1779a951ec7ab9482fc`
- 사용한 명령·주요 파라미터:
  - 고정 field plan: Constant master 31% / slave 32% → Sine master 35% / slave 40%
  - A=`master`, B=`async_slave`, `TimerON`, Frequency 30, guarded max 45%
  - 역할 write 완료 뒤 900초 complete epoch; 일시적 read·판정 오류는 진단으로만 기록
  - 종료 뒤 slave role detach → master role detach → TimerOFF → 원본 432-byte images →
    원 control/TimerON 순서
- 종료 지점(stage / failure code): `monitor_completed`,
  `verified=True`, `contradictory=False`, diagnostic issue 73. 이후 control-disarm의 한 read-back이
  불명확해 CLI는 exit 2 / `experiment_failed_restored`를 기록했지만 composed rollback은
  `outer_restored`, 네 journal은 none.
- 장비가 실제로 돌려준 값:
  - 경계 전 반복 sample: master `constant/31`, slave `constant/32`
  - 비원자 전환 첫 sample: master `constant/31`, slave `sine/40`
  - 수초 내 완전 수렴: master `sine/35`, slave `sine/40`
  - 이후 300초 안정 조건과 900초 epoch 끝까지 `35/40` 유지, 상충 sample 0
  - 따라서 slave는 master의 35%를 따라가지 않고 자기 B 슬롯의 40%를 적용했다.
  - 증거 등급:
    - epoch sample과 monitor 완료: **(b) preserved structured/durable daemon artifact** 및
      retained structured container log. 원본 wire frame은 아님.
    - 900초 전체를 실시간 확인한 사실: **(c) reconstructed operator observation**.
    - 원복 뒤 explicit reply와 두 452-byte status: **(a) preserved raw artifact**.
  - opaque artifact id 또는 안전한 상대 논리경로:
    - `hardware-safety/native-linkage-intent`
    - `docker-log:q2-attempt-05`
    - restore plan `JFP-dc4f80a9293721b5a2fe17fff2efdb76`
    - restore series `JFS-6d8d87542b82c6a8201bb671778452cf`
  - UTC span (시작 / 종료):
    - field run `2026-08-30T08:28:46.712086Z` / `2026-08-30T08:45:53.546402Z`
    - restore raw `2026-08-30T08:47:13.125523Z` / `2026-08-30T08:47:35.053183Z`
  - SHA-256 identity binding:
    - `4b60797d5fa86d879410c03a4c508665ee0f7a693fc54bd67c462c9a664bafea`
    - `97040a73bd12c3295dfb1cef77f6435949e78cc9e65bd2b28d93742e1e88cb1c`
  - artifact digest (SHA-256):
    - terminal intent:
      `6aa33443d1a20ae274b56888db1b5d7420d2d52910b32871ca01c99123632d57`
    - container log:
      `3ea8af3bb1595dd696e41ece65c69d45159bb96f167187c83cc97a6e1b3593dc`
    - restore plan:
      `a91d51d29859ec8ecedce7dd62bee3af6cc9d65493684b5bc58e4ad7d8a37651`
    - restore series:
      `92d540a0ef62a8f986912e72f3b0fdc7c80bc0ed5c99d0bc1e174456697eac68`
    - restore raw A/B:
      `593067d96e15dce3d39af7b7bab0bbb8900aabac053f05bc546965eef6251093`,
      `0e7f1cf9578626425724842d5730818f45c626005aea12ce8fba5deef8a3cfbb`
- 원복 검증 결과(어떤 수준의 확인이었는지 명시): fresh explicit raw에서 두 장비 모두
  `SwitchON`, `TimerON`, `independent`이고 원 manual Mode/Flow/Frequency와 현재 Auto tuple이
  baseline과 일치했다. 432-byte schedule SHA-256도 A
  `60edc697ebe4492259c6df01b9f92726af5648343f9b8e24950d721fa8f56b99`, B
  `c50331c20d220b2c51b2022863d29e5fc760d7ede71c299ec4f606e597d29426`로 각각 원본과
  byte-exact 일치했다. recovery supervisor는 새 코드로 재기동돼 running이다.
- **이 실행으로 새로 확정된 사실:** 이 두 Local Wavemaker Pro의 현재 펌웨어·설정에서는
  `async_slave`가 스케줄 경계에서 자기 슬롯의 독립 `AutoFlow`를 적용한다. Q2 판정은
  **YES / PASS (이 pair와 이 계획 범위)**다.
- 이 실행으로 확정하지 못한 것: 물리 유량·파형·위상, 다른 모델/펌웨어, manual Flow Q1,
  장시간 반복 경계의 신뢰도.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것: Q2 실기를 반복하지 말고 이 결과를
  capability로 기록한 뒤 software-independent actuator와 그룹 런타임 구현으로 이동한다.
