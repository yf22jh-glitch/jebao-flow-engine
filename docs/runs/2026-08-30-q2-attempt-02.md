# run: 2026-08-30-q2-attempt-02

- 목적(한 문장): 수정된 armed plan으로 Q2 A→B 경계 관측을 실행한다.
- 이 실행이 답하려던 질문: ASYNC slave가 자기 B 슬롯 `AutoFlow=40`을 적용하는가?
- 실행한 commit SHA: `d3e4bb420ac602e577978d7bed0a5bf64dc53c50`
- 사용한 명령·주요 파라미터: Constant 31/32 → Sine 35/40, guarded max 45, 단회
  expired-receipt exception.
- 종료 지점(stage / failure code): 임시 schedule 단계 뒤 nested role preflight authorization이
  exception을 받지 못해 role write 전 종료; `experiment_failed_restored`.
- 장비가 실제로 돌려준 값: A→B 경계 측정 없음.
  - 증거 등급: **(c) reconstructed operator observation**. retained CLI log는 sanitized
    failure 한 줄만 보존해 세부 stage를 독립 증명하지 못한다.
  - opaque artifact id 또는 안전한 상대 논리경로: `docker-log:q2-attempt-02`
  - UTC span (시작 / 종료): `2026-08-30T07:59:44.153630Z` /
    `2026-08-30T08:00:41.727862Z`
  - SHA-256 identity binding: 승인된 두 역할의 기존 binding
  - artifact digest (SHA-256):
    `30f52c6341b4a6d24eda0d2a4ee6254861e99e2ec6b10d1f75f1af024cd592bb`
- 원복 검증 결과(어떤 수준의 확인이었는지 명시): terminal, outer rollback completed, 네
  journal none을 당시 확인했다. 등급 (c).
- **이 실행으로 새로 확정된 사실:** receipt exception authority가 nested role spec에도
  전달돼야 한다.
- 이 실행으로 확정하지 못한 것: Q2 기능 동작.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것: outer spec과 nested role spec의 authority
  동등성을 테스트하고 confirmation token에 묶는다.
