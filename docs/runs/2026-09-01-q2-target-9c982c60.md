# run: 2026-09-01-q2-target-9c982c60

- 목적(한 문장): master와 slave에 반대 방향 Mode 스케줄을 넣어, native `async_slave`가
  master Mode·timing을 따르면서 자기 모드별 Flow `45 -> 60`을 적용하는지 한 번 판별한다.
- 이 실행이 답하려던 질문: "master가 `Sine/50/F40 -> Constant/55`로 전환할 때 slave가
  master의 `Sine -> Constant` Mode를 따르면서 자기 Flow `45 -> 60`을 적용하는가?"
- 실행한 commit SHA: `cbd622706a27d222268636d4c619641a51a431a8`
- 사용한 명령·주요 파라미터:
  - exact commit의 clean Docker image와 source-attested post-restore collector
  - master manual `30`, slave manual `35`, 승인 출력 범위 `30..60`
  - master A=`Sine/50/F40`, B=`Constant/55/wire F0`
  - slave A=`Constant/45/wire F0`, B=`Sine/60/F35`
  - 동일 device-local 경계, master=`master`, slave=`async_slave`, 목표 epoch `900초`
  - 현장 승인·감시와 물리 중단 수단을 확보하고, 다른 direct controller와 기존 recovery
    supervisor를 중지한 뒤 one-shot writer를 실행
- 종료 지점(stage / failure code):
  - outer safe pause: `2/2` 완료
  - sentinel schedule: snapshot `2/2`, write·verify `2/2`, exact restore `2/2` 완료
  - field schedule: snapshot `2/2` 완료 뒤 **첫 field schedule write 전에 종료**
  - durable outcome: `terminal / experiment_failed_restored`
  - 외부 오류: `LinkageApplyError`; durable forward category: `unexpected_failure`
  - rollback: `2026-09-01T00:37:45.282686Z` 시작,
    `2026-09-01T00:37:55.844613Z` 완료; rollback failure·recovery reason 없음
  - field write, `TimerON` arm, role write, ASYNC epoch와 900초 관측은 모두 미도달
- 장비가 실제로 돌려준 값:
  - 실행 중 role epoch에 도달하지 않아 Q2 판정용 raw frame은 `0`개다. private raw sink는
    header만 보존했다.
  - 실행 종료 뒤 서로 다른 프로세스·fresh session의 source-attested collector 2회를 수행했고,
    두 회 모두 `1/1 pair`, `2/2 explicit reply sample` accepted였다.
  - 두 collector raw를 오프라인 재검증한 결과 두 회 모두 다음 원 control과 일치했다.
    - master: `ON / TimerON / independent / constant / Flow 30 / Frequency 32`
    - slave: `ON / TimerON / independent / random / Flow 89 / Frequency 34`
  - 두 회 모두 원 schedule image digest와 일치했다.
    - master: `60edc697ebe4492259c6df01b9f92726af5648343f9b8e24950d721fa8f56b99`
    - slave: `c50331c20d220b2c51b2022863d29e5fc760d7ede71c299ec4f606e597d29426`
  - 증거 등급:
    - post-restore reply action·exact wire frame과 여기서 재디코딩한 control·schedule:
      **(a) preserved raw artifact**
    - terminal intent, stage event, rollback evidence, collector plan·series·host timing·identity
      binding: **(b) preserved structured/durable daemon artifact**
    - controller 중지·recovery supervisor 재기동·TCP connection 0 확인:
      **(c) reconstructed operator observation**
  - opaque artifact id와 digest:
    - live raw sink `JFR-9c982c606d67`: capture `0`,
      `3b87d9717820205ad670c444f933d6b9aabeb3ddf5adddb76f85990c74c398aa`
    - terminal intent logical id `native-linkage-intent`:
      `df1e119cd1baf312f6b7995c2d1ff67c1ceeaab49280c6e6b1fd2c197f25ab21`
    - verify 1 plan `JFP-fc9ebff009a345430475f2a594c12df9`:
      `437b7bc6aedd0210b98862ca8a7aa89a3acf7e0c0e9c7dd914ccaa4100fb0e3c`
    - verify 1 series `JFS-80bb8ef2c4a35934da86c5a4c12e81e1`:
      `310dd411480bb3591632781d98c2c0b95a3fd64a975fb49ea05b1b0a2db68dc3`
    - verify 2 plan `JFP-06049ac92d290fea55961546c0b13f0e`:
      `3e31540c0b6f1ac7a73a7b8fa8e6085ff5f95c9c2c381eb834d0c657ccaecff1`
    - verify 2 series `JFS-fb70cdf2c01ab2875c5f249980fc9c52`:
      `912483f66a4b33016dfa65d45e1958475d3f7cb0833d6acefa8a02e304fd021b`
  - UTC span (시작 / 종료):
    - live intent: `2026-09-01T00:37:18.365493Z` /
      `2026-09-01T00:37:55.850906Z`
    - live raw sink header 생성: `2026-09-01T00:37:28.271934Z`
    - verify 1: `2026-09-01T00:44:21.779214Z` /
      `2026-09-01T00:44:43.281921Z`
    - verify 2: `2026-09-01T00:45:04.737488Z` /
      `2026-09-01T00:45:26.272411Z`
  - SHA-256 identity binding(두 verify 공통):
    - `4b60797d5fa86d879410c03a4c508665ee0f7a693fc54bd67c462c9a664bafea`
    - `97040a73bd12c3295dfb1cef77f6435949e78cc9e65bd2b28d93742e1e88cb1c`
  - raw frame digest (SHA-256):
    - verify 1: `9b0c6e4bf48f672d0b6420fee37bcf77dba955ddb95d855715485482403e2037`,
      `670b90c564fd4d69d3b54a26f0a253c6ad8ecb82ee01a37558d2aca1a7887d0d`
    - verify 2: `fc424caff5b83fb9431cf6890a87471d0b1dd191b30c60157596c8a9c1595ecc`,
      `27818d1221b8cbe4e6493893c5d8f13a74ab75f647526c830688b21966439531`
  - pair completion gap: verify 1 `10,674.981 ms`, verify 2 `10,657.787 ms`
  - hardware-safety content-set digest: 실행 전
    `0c75ac8b76355b08258f130d1710262a64b3575c3abc0780f13c76a036aadd7e`, 실행 후
    `1373e23beb4dfeccfdde976a78de3e2bfd9a0c01f0e932eb44eafce9702cbc9b`.
    차이는 terminal intent 갱신과 header-only raw sink 추가를 포함하므로 동일성 주장이 아니다.
- 원복 검증 결과(어떤 수준의 확인이었는지 명시):
  - write-side durable intent가 outer rollback 완료와 journal 3종 `none`을 기록했다: **(b)**.
  - 별도 fresh collector 두 회에서 identity, explicit reply, 원 control 여섯 필드와 두
    432-byte schedule digest가 모두 일치했다: 장비 상태·schedule은 **(a)**, host timing과
    source attestation은 **(b)**.
  - matching recovery supervisor를 재기동했고 legacy recovery와 flow daemon은 정지 상태,
    established TCP `12416` connection은 `0`이었다: **(c)**.
- 오프라인 원인 재현:
  - exact persisted spec은 fixed Q2 signature와 일치한다.
  - 생성된 master topology는 `Sine -> Constant`, slave topology는
    `Constant -> Sine`이며 Flow는 각각 `50 -> 55`, `45 -> 60`이다.
  - `TemporaryScheduleController._expected_images()`의 기존 field guard는 두 장비 topology의
    완전 동일성을 요구한다. exact spec으로 같은 조건을 재계산하면
    `topology_equal=false / unsafe_initial_state`가 결정적으로 나온다.
  - durable stage가 field snapshot 완료 뒤 첫 field write 전에 끝난 사실과 결합하면,
    반대 Mode 계획을 만들면서 이 기존 동일-topology 전제를 지정 축소하지 않은 코드 내부
    충돌이 이번 종료 원인이다. 펌웨어 동작 가설이나 네트워크 오류가 아니다.
  - 단회 해제와 구현 전 감사는 `schedule_linkage.py`의 두 전제와 두 출력 상한을 열거했지만,
    `schedule_transaction.py`의 이 cross-device field-image guard는 열거·감사하지 않았다. 이
    감사 범위 누락이 실기 전에 구조적 충돌을 잡지 못한 직접 원인이다.
- **이 실행으로 새로 확정된 사실:**
  - 고정 Q2-target 계획은 outer safe pause와 sentinel qualification·restore까지 실장비에서
    완료하지만, 본 field schedule의 첫 write 전에 기존 동일-topology guard가
    `Sine -> Constant` 대 `Constant -> Sine` 계획을 거부한다.
  - field schedule과 native role write는 전송되지 않았으므로 이 실행은 Q2-target을 판정하지
    않는다.
  - 이미 전송된 outer pause·sentinel operation 뒤에도 write-side rollback과 독립 raw 검증
    2회에서 원 controls와 두 schedule image의 exact restore가 확인됐다.
  - outer pause와 sentinel write가 실제로 있었으므로 이 실행은 "첫 write 전 NO-GO"가 아니며,
    위 신규 사실을 만들었으므로 정보 0 실행도 아니다.
- 이 실행으로 확정하지 못한 것:
  - slave가 master Mode·timing을 따르는지와 모드별 Flow `45 -> 60`을 적용하는지는 계속
    `UNKNOWN`이다.
  - native ASYNC role, field A/B schedule, 경계, 900초 epoch는 실행되지 않았다.
  - 물리 유량·파형·위상과 장기 반복 신뢰도는 측정하지 않았다.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것:
  - 이번 단회 write 승인은 이 실행으로 소진한다. 같은 operation을 재시도하지 않는다.
  - 대안 A: repository maintainer가 Q2-target의 추가 가치가 있다고 판단할 때만 별도 동결
    해제 커밋으로, exact fixed signature에 한해 `schedule_transaction.py`의 동일-topology 전제를
    판별 계획에 맞게 지정 축소 대상으로 명시하고 **장비 write 없는** exact-image 경로로 먼저
    재현한다. 향후 승인 문서와 구현 감사는 이 guard를 이름으로 열거해야 한다.
  - 대안 B: Q2-target을 `UNKNOWN (PARKED)`으로 유지하고, 답과 무관하게 필요한
    software-independent actuator와 그룹 런타임을 우선 구현한다.
