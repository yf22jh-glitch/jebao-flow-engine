# run: 2026-08-30-baseline-a9e40866

- 목적(한 문장): Q2 실기 재개 직전에 현재 두 Pro의 exact-restore admission을 새
  source-attested explicit raw pair로 다시 확인한다.
- 이 실행이 답하려던 질문: "역할 B의 latent manual `Flow=89`가 현재도 남아 있어
  standalone exact-restore qualification을 막는가?"
- 실행한 commit SHA: `708fed7cc42e3e1efb3b6abf1bd4fd84762f249e`
- 사용한 명령·주요 파라미터:
  - exact commit의 clean source checkout, external dependency target, project 미설치
  - `PYTHONNOUSERSITE=1`, `PYTHONDONTWRITEBYTECODE=1`, `python3.12 -B -P`
  - owner-only private config·artifact root
  - 두 논리 역할, `--samples 1 --cadence-seconds 30 --timeout 5`
  - 실행 중 recovery supervisor를 중지하고 종료 직후 재기동
  - runtime source digest:
    `722239307c043cdf1c4fe4d04fdeac2fd323827eaf951719afc6778856106698`
- 종료 지점(stage / failure code): `pilot_completed_all_acquisitions_accepted`, exit 0
- 장비가 실제로 돌려준 값:
  - 1/1 pair, 2/2 sample accepted; rejected 0, read failure 0
  - 두 sample 모두 explicit state reply와 452-byte Pro status 조건을 통과
  - pair completion gap: `11,287.7161 ms`
  - 역할 A: `ON`, `TimerON`, `independent`, manual `constant / Flow=30 /
    Frequency=32`, 현재 `AutoMode=sine / AutoFlow=60 / AutoFreq=15`, active non-feed
    schedule Flow `30..60`
  - 역할 B: `ON`, `TimerON`, `independent`, manual `random / Flow=89 /
    Frequency=34`, 현재 `AutoMode=pulse / AutoFlow=80 / AutoFreq=70`, active non-feed
    schedule Flow `50..80`
  - 역할 B exact-restore admission: **FAIL (`89 > 80`)**
  - 증거 등급:
    - reply action·status exact bytes와 위 장비 값: **(a) preserved raw artifact**
    - source attestation, host timing, identity binding, plan·series claim:
      **(b) preserved structured/durable daemon artifact**
  - opaque artifact id:
    - plan: `JFP-8fafc42f18045cb3a060095b15eebdc8`
    - series: `JFS-a9e4086683e60d859faca6a04d80729a`
  - UTC span (시작 / 종료): `2026-08-30T07:05:54.775139Z` /
    `2026-08-30T07:06:17.051311Z`
  - SHA-256 identity binding:
    - `4b60797d5fa86d879410c03a4c508665ee0f7a693fc54bd67c462c9a664bafea`
    - `97040a73bd12c3295dfb1cef77f6435949e78cc9e65bd2b28d93742e1e88cb1c`
  - artifact digest (SHA-256):
    - plan: `353a332cf2e2eeebe0a1cb719c1fd3f5e82bf0b89a31ac03ddda994939861dc5`
    - series: `303cb4d05f4f3d6bb6ddc2bd073ff1e53c623dd1fe6142de3a452f03b7709ae5`
  - raw frame digest (SHA-256):
    - 역할 A: `38070315ff3fafa6e063754528c05065bde6127c57ffdcc3d3d3382eb49c6033`
    - 역할 B: `5c6979908fdd59f4e6aab6ce64463c0983be19475964ffb2936b0bd0e0af0790`
- 오프라인 검증:
  - collector가 plan, 완전한 ordinal 집합, sample·pair·raw digest, identity binding,
    explicit reply와 terminal marker를 검증하고 완료 상태를 fsync
  - 동일 raw에서 manual controls, 현재 `Auto*`, 48-slot schedule과 schedule digest를 재디코딩
- 원복 검증 결과(어떤 수준의 확인이었는지 명시): write-free acquisition이라 장비 원복 대상은
  없다. 중지했던 recovery supervisor가 다시 running이고 exact-restore journal은 `idle`임을
  별도로 확인했다. 이 운영 상태 확인은 **(c) reconstructed operator observation**이다.
- **이 실행으로 새로 확정된 사실:**
  - 역할 B의 latent manual `Flow=89`는 `708fed7`의 fresh source-attested raw에서도 유지된다.
  - 두 역할의 linkage·schedule image는 앞선 2026-08-30 preserved baseline과 동일하고,
    현재 `Auto*` 값만 당일 시간표에 따라 진행됐다.
  - 따라서 앱에서 B manual Flow를 안전 범위로 정규화하고 새 baseline을 보존하기 전에는
    standalone exact-restore qualification을 시작할 수 없다.
- 이 실행으로 확정하지 못한 것:
  - Q2의 ASYNC 슬롯별 독립 적용 여부는 측정하지 않았으며 계속 `UNKNOWN (PARKED)`이다.
  - 앱 live-write, sentinel qualification, exact restore와 두 measurement epoch는 실행하지 않았다.
  - 물리 유량·파형·위상은 측정하지 않았다.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것:
  - 역할 B manual Flow를 승인된 앱 operation으로 80 이하에 정규화
  - 앱 완전 종료 뒤 새 source-attested baseline 수집
  - 새 baseline을 근거로 exact-restore qualification과 Q2 두 epoch 실행
