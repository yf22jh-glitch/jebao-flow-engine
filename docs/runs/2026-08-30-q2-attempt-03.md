# run: 2026-08-30-q2-attempt-03

- 목적(한 문장): 두 역할을 실제 적용해 Q2 스케줄 경계까지 관측한다.
- 이 실행이 답하려던 질문: ASYNC slave가 자기 B 슬롯 `AutoFlow=40`을 적용하는가?
- 실행한 commit SHA: `bd018fef6aadf59afa03d4151588ec0cb9d7d86c`
- 사용한 명령·주요 파라미터: Constant 31/32 → Sine 35/40, guarded max 45, nested
  expired-receipt exception.
- 종료 지점(stage / failure code): master와 async-slave role write 뒤 slave pair state read가
  실패해 monitor 진입 전에 조기 원복; `slave_pair_state_read`,
  `experiment_failed_restored`.
- 장비가 실제로 돌려준 값: schedule 경계 sample 없음.
  - 증거 등급: **(c) reconstructed operator observation**. retained CLI log는 sanitized
    failure만 보존한다.
  - opaque artifact id 또는 안전한 상대 논리경로: `docker-log:q2-attempt-03`
  - UTC span (시작 / 종료): `2026-08-30T08:03:32.801733Z` /
    `2026-08-30T08:05:51.333678Z`
  - SHA-256 identity binding: 승인된 두 역할의 기존 binding
  - artifact digest (SHA-256):
    `30f52c6341b4a6d24eda0d2a4ee6254861e99e2ec6b10d1f75f1af024cd592bb`
- 원복 검증 결과(어떤 수준의 확인이었는지 명시): terminal, outer rollback completed, 네
  journal none을 당시 확인했다. 등급 (c).
- **이 실행으로 새로 확정된 사실:** role adapter write가 성공한 뒤의 일시적 state read 실패를
  즉시 원복 사유로 쓰면 Q2 측정 채널이 다시 닫힌다.
- 이 실행으로 확정하지 못한 것: Q2 기능 동작.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것: 단회 complete epoch에서는 acknowledged
  role write 뒤의 비안전 진단 오류를 기록하고 monitor로 계속 진행한다.
