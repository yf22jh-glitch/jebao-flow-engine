# run: 2026-08-30-preflight-8e4204d2

- 목적(한 문장): exact-restore composition 작업 전에 현재 두 Pro의 baseline admission을
  source-attested explicit raw pair로 다시 확인한다.
- 이 실행이 답하려던 질문: "보존된 2026-08-30 baseline에서 관측된 역할 B의 latent
  `Flow=89`가 현재 장비에도 남아 있어 standalone restore qualification을 계속 막는가?"
- 실행한 commit SHA: `a6d472ec2617b520768a25275fa585a3740b49ec`
- 사용한 명령·주요 파라미터:
  - exact commit의 detached clean source를 read-only mount하고 그 source module을 직접 실행
  - private locked config, 두 논리 역할, owner-only private artifact root
  - `--samples 1 --cadence-seconds 30 --timeout 5`
  - config는 `runtime.dry_run=true`, 모든 장비 `allow_hardware_writes=false`
  - 실행 중 서버의 Jebao 관련 container는 recovery supervisor 하나였고 collector는 장비
    control API를 import하거나 호출하지 않음
- 종료 지점(stage / failure code): `pilot_completed_all_acquisitions_accepted`, exit 0
- 장비가 실제로 돌려준 값:
  - 1/1 pair, 2/2 sample accepted; rejected 0, read failure 0
  - 두 sample 모두 explicit state reply와 452-byte Pro status 조건을 통과
  - 역할 A: `ON`, `TimerON`, `independent`, manual `constant / Flow=30 /
    Frequency=32`, active non-feed schedule Flow `30..60`
  - 역할 B: `ON`, `TimerON`, `independent`, manual `random / Flow=89 /
    Frequency=34`, active non-feed schedule Flow `50..80`
  - 역할 B admission: **FAIL (`89 > 80`)**
  - 증거 등급:
    - reply action·status exact bytes와 위 장비 값: **(a) preserved raw artifact**
    - source attestation, host timing, identity binding, plan·series claim: **(b) preserved
      structured/durable daemon artifact**
  - opaque artifact id:
    - plan: `JFP-e2f278b43d79de8338a32ba5197a3099`
    - series: `JFS-8e4204d2092e6ef017d09c7fd39d90e1`
  - UTC span (시작 / 종료): `2026-08-30T04:27:36.475835Z` /
    `2026-08-30T04:27:58.819909Z`
  - SHA-256 identity binding:
    - `4b60797d5fa86d879410c03a4c508665ee0f7a693fc54bd67c462c9a664bafea`
    - `97040a73bd12c3295dfb1cef77f6435949e78cc9e65bd2b28d93742e1e88cb1c`
  - artifact digest (SHA-256):
    - plan: `c3cec590e0305e69fc6cf02220c91e3deaebe09853e44d3c8289c220a6e27e59`
    - series: `10887348fd0aae38adb9e281027d6fdd36375e093715321b00e0b2d89b51ef50`
  - raw frame digest (SHA-256):
    - 역할 A: `580e9ea3168f8bfd669af8bced0683d8bae69a61735e20a7a4ffb482b617c9d0`
    - 역할 B: `0f95c3521ff78aafcc4b351575ece71afa74cb474ab38203fb52d2b1d9de64c9`
- 오프라인 검증:
  - 같은 exact commit의 `PilotSeriesStore.verify_completed_series()`가 plan, 완전한 ordinal 집합,
    sample·pair·raw digest, identity binding, explicit reply와 terminal marker를 재계산해 PASS
  - 검증 뒤 동일 raw frame에서 outer controls와 48-slot schedule을 다시 디코딩해 위 범위를 도출
- 원복 검증 결과(어떤 수준의 확인이었는지 명시): write-free acquisition이라 원복 대상 없음.
  장비 control frame은 전송하지 않았다.
- **이 실행으로 새로 확정된 사실:**
  - 역할 B의 latent manual `Flow=89`가 현재 fresh raw에도 남아 있고 active non-feed schedule
    ceiling은 80이므로, 현 baseline으로 exact-restore qualification을 실행할 수 없다.
  - 역할 A/B는 현재 둘 다 `ON + TimerON + independent`이고 두 schedule image digest는 앞선
    30초 cadence artifact와 각각 동일하다.
- 이 실행으로 확정하지 못한 것:
  - Q2의 ASYNC 슬롯별 독립 적용 여부는 측정하지 않았으며 계속 `UNKNOWN (PARKED)`이다.
  - 앱 live-write, sentinel qualification, exact restore와 두 measurement epoch는 실행하지 않았다.
  - 물리 유량·파형·위상은 측정하지 않았다.
- 다음에 이 지점을 다시 만나지 않으려면 필요한 것:
  - 역할 B를 80 이하로 낮추는 별도 현장 안전정규화 operation의 승인·감시·물리 차단 수단
  - 정규화 뒤 앱 완전 종료와 새 source-attested raw baseline 보존
  - verified artifact composition, concrete writer, attended recovery CLI와 cross-workflow admission
